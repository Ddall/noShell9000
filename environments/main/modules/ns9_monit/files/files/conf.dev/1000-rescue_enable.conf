check file rescue.inactive with path /var/rescue/rescue.inactive
    group rescue
    if timestamp > 2 minutes
        then alert
    if does not exist
        then exec "/bin/bash /var/rescue/scripts/enableRescue.sh >> /var/rescue/enableRescue.log 2>&1"